# Changelog

## 0.4.1

- 修复房间被误删：房主在房间里换音频设备走的是 `leave → join`，中间有几秒谁都不在；以前这段空窗会把房间直接删掉，而房主的局域网广播还挂着，队友就成了「发现得到这台机器、却找不到房间」。现在留 20 秒宽限期，重进不受影响，房主真关了 App 也最多多活 20 秒
- 信令层错误不再消失：「房间不存在」「目标不在线」以前只写日志，而桌面端没装日志订阅器，等于被扔进黑洞——用户只看到一个和真实原因无关的界面。现在带 30 秒时效显示在房间页
- 建房时局域网广播失败不再静默吞掉：以前广播挂了房主毫无察觉，队友却怎么也发现不到他，两边看到的现象互相矛盾
- 发版修复：`latest.json` 里的下载地址不再取草稿期 URL。**v0.4.0 的清单里所有地址都是草稿路径，Release 转正后一律 404，客户端一个包都下不动**——升级到 0.4.1 即可恢复自动更新

## 0.4.0

- 局域网自动发现：同一个 Wi-Fi 下输房号即可，不用填任何地址（mDNS，只有正在主持房间的那台广播；不做无条件暴露）
- 邀请串 `vivi://<房号>@<host>:<port>`：异地由房主点「邀请队友」把地址发给对方，粘进房间号框即可；同是合法 URL，为将来注册 `vivi://` 协议处理器留好解析
- 内嵌信令端口被占不再静默放弃：8080 被占用时先探一下是不是自己的信令，是就复用，不是就往后试 8081–8089，全被占则在首页明确报「本机信令没起来」
- TURN：没配 `TURN_URLS` 就不签发凭证。以前默认兜 `turn:127.0.0.1:3478`，等于告诉客户端「你的 TURN 在自己本机」，必然分配失败还要白等一轮超时
- STUN 服务器按国内出口实测重挑（Google / Cloudflare / miwifi / bilibili 可达，`stun.qq.com`、`stun.syncthing.net` 超时已移除）
- 发版流水线：清单生成遇到认不出的安装包会直接报错，并新增公网自检，避免整个平台被静默跳过
- 新增 `docs/NETWORK.md`：跨机联调、mDNS 原理与失效场景、STUN/TURN 配置与排查

## 0.3.1

- Windows 试麦修复：采集/播放支持 U8 等更多采样格式（部分 USB/虚拟设备只给 U8，之前直接报错失败）；选设备时跳过不可驱动的格式

## 0.3.0

- 重命名：GameVoice → Vivi（窗口标题/安装包/bundle id `com.vivi.desktop`，旧偏好与信令地址自动迁移；bundle id 变更需手动安装一次）
- 设置页：底部显示当前版本与更新状态，支持手动检查更新/更新并重启；首页底部同步显示版本号
- Windows↔Mac 互通：采集/播放做采样率重采样归一到 48k（Windows 常见 44100Hz 之前会直接进不了房），两端设备采样率不同也能通话

## 0.2.2

- 跨机联通：内嵌信令监听 0.0.0.0，首页可配信令地址（两台指向房主），入会前预检房间存在，房号自动规范化
- 界面重做：窗口内无滚动条（固定页眉页脚 + 内部滚动），试麦大卡片改为紧凑状态条，整体去玩具感
- 文案：用户侧错误信息不再出现开发者术语（如 tauri dev）

## 0.2.1

- macOS：打包时对 .app 做 ad-hoc 签名，避免 Sequoia 把从网上下载的安装包显示成「已损坏」

## 0.2.0

- 应用图标：海军底上的琥珀麦，直播信号圈
- 首页与窗口使用同一套品牌标
